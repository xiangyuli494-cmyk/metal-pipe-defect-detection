# 金属细管内壁缺陷检测系统 - Python后端依赖
# 2026-09-15 更新：版本适配 Python 3.10~3.12，全部按实测通过版本锁定下限

# Web框架
fastapi>=0.104
uvicorn[standard]>=0.24
python-multipart>=0.0.6

# 数据库
sqlalchemy>=2.0.23
pymysql>=1.1.0
cryptography>=41.0
supabase>=2.0.3

# AI/ML（推理必需，勿注释）
# 无 NVIDIA 显卡的电脑可加装 CPU 版以减小体积：
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
torch>=2.1
torchvision>=0.16
ultralytics>=8.1

# 图像处理（4.8 及以下无 Python 3.12 轮子，故下限 4.10）
pillow>=10.1
opencv-python>=4.10
numpy>=1.26,<3

# 认证
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt>=4.0.1

# 工具库
pydantic>=2.5
python-dotenv>=1.0
requests>=2.31
aiofiles>=23.2

# 开发工具
pytest>=7.4
pytest-asyncio>=0.21
httpx>=0.25
